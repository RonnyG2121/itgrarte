// Bucles
const ALUMNOS = [
    "Juan",
    "Carla",
    "Pedro",
    "María"
];

// sintaxis del bucle for

/* for (let i = 0; i< ALUMNOS.length; i++) {
    console.info(i);
    console.info(ALUMNOS[i]);
}
 */



// Añadir elementos al arreglo
// ALUMNOS.push("Lucas");

// console.log(ALUMNOS[4]);


// Sintaxis de bucle while
// Bucle que no se ejecuta
let numero = 0;
/* while (numero> 5) {
    console.info(numero);
} */

// Bucle para hacer un conte

/* while (numero <5) {
    numero++;
    console.log(numero);
}
 */

// Usar la instrucción continue

/* while (numero <10) {
    numero++;
    if (numero === 4) {
        continue;
    }

    console.info(numero);
} */

    // break
/* while (numero <10) {
    numero++;
    if (numero === 5) {
        break;
    }
    console.info(numero);
} */

    